# 📚 Покрокова інструкція по створенню та налаштуванню проєкту Laravel

## 📋 Зміст
1. [Встановлення Laravel](#встановлення-laravel)
2. [Початкове налаштування](#початкове-налаштування)
3. [Реалізація MVC](#реалізація-mvc)
4. [Визначення термінів](#визначення-термінів)

---

## 🚀 Встановлення Laravel

### Крок 1: Перевірка вимог
Переконайтеся, що на вашому комп'ютері встановлено:
- **PHP** версії 8.1 або вище
- **Composer** (менеджер залежностей PHP)
- **MySQL/PostgreSQL** або інша база даних
- Розширення PHP: `mbstring`, `openssl`, `pdo`, `tokenizer`, `xml`, `ctype`, `json`, `bcmath`

### Крок 2: Встановлення через Composer
Відкрийте термінал і виконайте команду:

```bash
composer create-project laravel/laravel назва-проєкту
```

Або для глобальної установки Laravel Installer:

```bash
composer global require laravel/installer
laravel new назва-проєкту
```

### Крок 3: Перехід у директорію проєкту
```bash
cd назва-проєкту
```

---

## ⚙️ Початкове налаштування

### Крок 1: Налаштування змінних оточення
1. Скопіюйте файл `.env.example` у `.env`:
   ```bash
   cp .env.example .env
   ```

2. Згенеруйте ключ додатку:
   ```bash
   php artisan key:generate
   ```

### Крок 2: Налаштування бази даних
Відкрийте файл `.env` та налаштуйте підключення до бази даних:

```env
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=назва_бази_даних
DB_USERNAME=ваш_користувач
DB_PASSWORD=ваш_пароль
```

### Крок 3: Запуск міграцій (якщо потрібно)
```bash
php artisan migrate
```

### Крок 4: Запуск сервера розробки
```bash
php artisan serve
```

Проєкт буде доступний за адресою: `http://localhost:8000`

---

## 🏗️ Реалізація MVC

### Крок 1: Створення міграції (Migration)

**Міграція** - це файл, який описує структуру таблиці в базі даних.

```bash
php artisan make:migration create_products_table
```

Відкрийте створений файл у `database/migrations/` та додайте поля:

```php
public function up()
{
    Schema::create('products', function (Blueprint $table) {
        $table->id();
        $table->string('name');
        $table->text('description');
        $table->decimal('price', 8, 2);
        $table->timestamps();
    });
}
```

Запустіть міграцію:
```bash
php artisan migrate
```

### Крок 2: Створення моделі (Model)

**Модель** - це клас, який представляє таблицю бази даних та дозволяє працювати з даними.

```bash
php artisan make:model Product
```

Модель буде створена у `app/Models/Product.php`:

```php
<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Model;

class Product extends Model
{
    protected $fillable = ['name', 'description', 'price'];
}
```

### Крок 3: Створення фабрики (Factory)

**Фабрика** - це клас для генерації тестових даних для моделі.

```bash
php artisan make:factory ProductFactory
```

Відкрийте `database/factories/ProductFactory.php`:

```php
public function definition(): array
{
    return [
        'name' => fake()->words(3, true),
        'description' => fake()->paragraph(),
        'price' => fake()->randomFloat(2, 10, 1000),
    ];
}
```

### Крок 4: Створення сідера (Seeder)

**Сідер** - це клас для заповнення бази даних початковими даними.

```bash
php artisan make:seeder ProductSeeder
```

Відкрийте `database/seeders/ProductSeeder.php`:

```php
public function run(): void
{
    Product::factory()->count(10)->create();
}
```

Запустіть сідер:
```bash
php artisan db:seed --class=ProductSeeder
```

### Крок 5: Створення контролера (Controller)

**Контролер** - це клас, який обробляє HTTP-запити та повертає відповіді.

```bash
php artisan make:controller ProductController
```

Відкрийте `app/Http/Controllers/ProductController.php`:

```php
<?php

namespace App\Http\Controllers;

use App\Models\Product;
use Illuminate\Http\Request;

class ProductController extends Controller
{
    public function index()
    {
        $products = Product::all();
        return view('products.index', compact('products'));
    }

    public function show(Product $product)
    {
        return view('products.show', compact('product'));
    }
}
```

### Крок 6: Створення файлу шаблону (View)

**View (Шаблон)** - це файл, який відображає HTML-розмітку для користувача.

Створіть директорію:
```bash
mkdir -p resources/views/products
```

Створіть файл `resources/views/products/index.blade.php`:

```blade
@extends('layouts.app')

@section('content')
    <h1>Список продуктів</h1>
    @foreach($products as $product)
        <div>
            <h3>{{ $product->name }}</h3>
            <p>{{ $product->description }}</p>
            <p>Ціна: {{ $product->price }} грн</p>
        </div>
    @endforeach
@endsection
```

### Крок 7: Створення маршруту (Route)

**Маршрут** - це правило, яке визначає, який контролер обробляє певний URL.

Відкрийте `routes/web.php`:

```php
use App\Http\Controllers\ProductController;

Route::get('/products', [ProductController::class, 'index'])->name('products.index');
Route::get('/products/{product}', [ProductController::class, 'show'])->name('products.show');
```

---

## 📖 Визначення термінів

### 🗄️ Міграція (Migration)
**Міграція** - це файл, який описує зміни в структурі бази даних. Він дозволяє версіонувати схему бази даних та легко відкочувати зміни. Міграції забезпечують синхронізацію структури БД між різними середовищами (розробка, тестування, продакшн).

**Приклад:** Створення таблиці `users` з полями `id`, `name`, `email`.

### 🎯 Модель (Model)
**Модель** - це клас, який представляє таблицю бази даних та інкапсулює логіку роботи з даними. Модель дозволяє виконувати CRUD операції (Create, Read, Update, Delete) без написання SQL-запитів вручну. Вона використовує ORM (Object-Relational Mapping) для взаємодії з базою даних.

**Приклад:** Модель `User` представляє таблицю `users` та дозволяє отримувати, створювати, оновлювати та видаляти користувачів.

### 🏭 Фабрика (Factory)
**Фабрика** - це клас, який використовується для генерації тестових або демонстраційних даних. Вона дозволяє швидко створювати фейкові записи в базі даних з реалістичними значеннями для тестування та розробки.

**Приклад:** `UserFactory` може генерувати користувачів з випадковими іменами, email-адресами та паролями.

### 🌱 Сідер (Seeder)
**Сідер** - це клас, який заповнює базу даних початковими або тестовими даними. Він використовує фабрики для створення записів та дозволяє ініціалізувати базу даних необхідними даними для роботи додатку.

**Приклад:** `DatabaseSeeder` може створити 100 користувачів, 50 продуктів та 10 категорій для тестування.

### 🎮 Контролер (Controller)
**Контролер** - це клас, який обробляє HTTP-запити від клієнта та повертає відповіді. Він містить методи (actions), які виконують бізнес-логіку, взаємодіють з моделями та повертають view або JSON-відповіді.

**Приклад:** `ProductController` має методи `index()` (список продуктів) та `show()` (один продукт), які обробляють GET-запити.

### 🎨 View (Шаблон)
**View (Шаблон)** - це файл, який містить HTML-розмітку та Blade-синтаксис для відображення даних користувачу. Він отримує дані від контролера та відображає їх у зручному форматі. Blade - це шаблонізатор Laravel, який дозволяє використовувати PHP-код у HTML.

**Приклад:** `products/index.blade.php` відображає список всіх продуктів у вигляді HTML-таблиці або карток.

### 🛣️ Маршрут (Route)
**Маршрут** - це правило, яке визначає зв'язок між URL-адресою та методом контролера. Він вказує, який контролер та який його метод обробляють певний HTTP-запит (GET, POST, PUT, DELETE тощо).

**Приклад:** Маршрут `/products` пов'язаний з методом `index()` контролера `ProductController`, який відображає список продуктів.

---

## 📝 Підсумок

Після виконання всіх кроків ви матимете:
- ✅ Встановлений Laravel проєкт
- ✅ Налаштовану базу даних
- ✅ Міграцію для таблиці `products`
- ✅ Модель `Product`
- ✅ Фабрику для генерації тестових даних
- ✅ Сідер для заповнення БД
- ✅ Контролер для обробки запитів
- ✅ View для відображення даних
- ✅ Маршрути для доступу до сторінок

---

## 🔗 Корисні команди Laravel

```bash
# Створення міграції
php artisan make:migration create_table_name

# Запуск міграцій
php artisan migrate

# Відкат останньої міграції
php artisan migrate:rollback

# Створення моделі
php artisan make:model ModelName

# Створення контролера
php artisan make:controller ControllerName

# Створення фабрики
php artisan make:factory FactoryName

# Створення сідера
php artisan make:seeder SeederName

# Запуск сідера
php artisan db:seed

# Очищення кешу
php artisan cache:clear
php artisan config:clear
php artisan route:clear
php artisan view:clear
```

---

**Примітка:** Ця інструкція створена для Laravel 10.x. Для інших версій деякі команди можуть відрізнятися.

