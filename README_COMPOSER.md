# Composer PHP Project

Цей проект демонструє використання Composer для управління залежностями в PHP.

## 📦 Встановлені пакети

### 1. Monolog (monolog/monolog)
Потужна бібліотека для логування з підтримкою різних handler'ів та formatter'ів.

**Використання:**
```php
use Monolog\Logger;
use Monolog\Handler\StreamHandler;

$logger = new Logger('app');
$logger->pushHandler(new StreamHandler('logs/app.log', Logger::DEBUG));
$logger->info('Повідомлення');
```

### 2. Symfony VarDumper (symfony/var-dumper)
Красивий вивід змінних для дебагу з підтримкою HTML та CLI форматування.

**Використання:**
```php
use Symfony\Component\VarDumper\VarDumper;

dump($variable); // Красивий вивід
dd($variable);    // Вивід та зупинка виконання
```

### 3. PHP Dotenv (vlucas/phpdotenv)
Робота з змінними оточення через .env файли. Безпечне зберігання конфігурації.

**Використання:**
```php
use Dotenv\Dotenv;

$dotenv = Dotenv::createImmutable(__DIR__);
$dotenv->load();

$dbHost = $_ENV['DB_HOST'];
```

## 🚀 Встановлення

1. **Встановіть Composer** (якщо ще не встановлено):
   ```bash
   # Windows
   https://getcomposer.org/download/
   
   # Linux/Mac
   curl -sS https://getcomposer.org/installer | php
   ```

2. **Встановіть залежності:**
   ```bash
   composer install
   ```

3. **Налаштуйте .env файл:**
   ```bash
   cp .env.example .env
   # Відредагуйте .env файл зі своїми значеннями
   ```

## 📝 Використання

### Запуск демонстрації через веб-браузер:
```bash
php -S localhost:8000
# Відкрийте http://localhost:8000/composer-demo
```

### Запуск консольного демо:
```bash
php composer_demo.php
```

## 📁 Структура проекту

```
/
├── composer.json          # Конфігурація Composer
├── composer.lock          # Зафіксовані версії пакетів
├── vendor/                # Встановлені пакети (не комітиться)
├── src/                   # Власний код проекту
│   ├── LoggerExample.php
│   ├── VarDumperExample.php
│   └── DotEnvExample.php
├── logs/                  # Логи (створюється автоматично)
├── .env                   # Змінні оточення (не комітиться)
├── .env.example           # Приклад .env файлу
└── composer_demo.php      # Демонстраційний скрипт
```

## 🔧 Git Гілка

Проект знаходиться в окремій гілці:
```bash
git checkout composer-project
```

## 📚 Документація пакетів

- **Monolog:** https://github.com/Seldaek/monolog
- **VarDumper:** https://symfony.com/doc/current/components/var_dumper.html
- **PHP Dotenv:** https://github.com/vlucas/phpdotenv

## ⚠️ Важливо

1. **Не комітьте** файл `.env` в git (він вже в .gitignore)
2. **Не комітьте** папку `vendor/` (встановлюється через composer install)
3. Використовуйте `.env.example` як шаблон для налаштування

## 🎯 Приклади використання

### Логування з Monolog:
```php
$logger->debug('Debug повідомлення');
$logger->info('Інформаційне повідомлення');
$logger->warning('Попередження');
$logger->error('Помилка');
```

### Вивід змінних з VarDumper:
```php
dump($array);        // Вивід без зупинки
dd($object);         // Вивід з зупинкою виконання
```

### Робота з .env:
```php
$appName = $_ENV['APP_NAME'];
$debug = filter_var($_ENV['APP_DEBUG'], FILTER_VALIDATE_BOOLEAN);
```

---

**Автор:** Kostiantyn Surkov  
**Дата:** 2024

