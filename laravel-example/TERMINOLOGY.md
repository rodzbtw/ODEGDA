# 📖 Визначення термінів Laravel MVC

## 🗄️ Міграція (Migration)

**Міграція** - це файл, який описує зміни в структурі бази даних. Він дозволяє версіонувати схему бази даних та легко відкочувати зміни. Міграції забезпечують синхронізацію структури БД між різними середовищами (розробка, тестування, продакшн).

### Основні характеристики:
- Зберігаються в `database/migrations/`
- Мають два методи: `up()` (застосування) та `down()` (відкат)
- Виконуються командою `php artisan migrate`
- Можна відкотити командою `php artisan migrate:rollback`

### Приклад:
```php
Schema::create('users', function (Blueprint $table) {
    $table->id();
    $table->string('name');
    $table->string('email')->unique();
    $table->timestamps();
});
```

---

## 🎯 Модель (Model)

**Модель** - це клас, який представляє таблицю бази даних та інкапсулює логіку роботи з даними. Модель дозволяє виконувати CRUD операції (Create, Read, Update, Delete) без написання SQL-запитів вручну. Вона використовує ORM (Object-Relational Mapping) для взаємодії з базою даних.

### Основні характеристики:
- Зберігаються в `app/Models/`
- Наслідуються від `Illuminate\Database\Eloquent\Model`
- Автоматично пов'язані з таблицею БД (назва класу в множині)
- Підтримують масове заповнення через `$fillable` або `$guarded`

### Приклад:
```php
$user = User::find(1);              // Знайти користувача з id=1
$user->name = 'Нове ім\'я';         // Змінити ім'я
$user->save();                      // Зберегти зміни

User::create(['name' => 'Іван']);    // Створити нового користувача
User::where('active', true)->get(); // Отримати всіх активних
```

---

## 🏭 Фабрика (Factory)

**Фабрика** - це клас, який використовується для генерації тестових або демонстраційних даних. Вона дозволяє швидко створювати фейкові записи в базі даних з реалістичними значеннями для тестування та розробки.

### Основні характеристики:
- Зберігаються в `database/factories/`
- Використовують бібліотеку Faker для генерації даних
- Мають метод `definition()` для визначення структури даних
- Можуть мати стани (states) для різних варіантів даних

### Приклад:
```php
Product::factory()->count(10)->create();           // Створити 10 продуктів
Product::factory()->inactive()->create();          // Створити неактивний продукт
Product::factory()->expensive()->count(5)->create(); // Створити 5 дорогих продуктів
```

---

## 🌱 Сідер (Seeder)

**Сідер** - це клас, який заповнює базу даних початковими або тестовими даними. Він використовує фабрики для створення записів та дозволяє ініціалізувати базу даних необхідними даними для роботи додатку.

### Основні характеристики:
- Зберігаються в `database/seeders/`
- Мають метод `run()` для виконання заповнення
- Виконуються командою `php artisan db:seed`
- Можуть використовувати фабрики або створювати дані вручну

### Приклад:
```php
public function run(): void
{
    User::factory()->count(100)->create();
    Product::factory()->count(50)->create();
}
```

---

## 🎮 Контролер (Controller)

**Контролер** - це клас, який обробляє HTTP-запити від клієнта та повертає відповіді. Він містить методи (actions), які виконують бізнес-логіку, взаємодіють з моделями та повертають view або JSON-відповіді.

### Основні характеристики:
- Зберігаються в `app/Http/Controllers/`
- Наслідуються від `Illuminate\Http\Controllers\Controller`
- Методи називаються actions (дії)
- Можуть повертати view, JSON, редиректи тощо

### Типові методи (CRUD):
- `index()` - список ресурсів
- `show($id)` - один ресурс
- `create()` - форма створення
- `store(Request $request)` - збереження нового
- `edit($id)` - форма редагування
- `update(Request $request, $id)` - оновлення
- `destroy($id)` - видалення

### Приклад:
```php
public function index()
{
    $products = Product::all();
    return view('products.index', compact('products'));
}
```

---

## 🎨 View (Шаблон)

**View (Шаблон)** - це файл, який містить HTML-розмітку та Blade-синтаксис для відображення даних користувачу. Він отримує дані від контролера та відображає їх у зручному форматі. Blade - це шаблонізатор Laravel, який дозволяє використовувати PHP-код у HTML.

### Основні характеристики:
- Зберігаються в `resources/views/`
- Мають розширення `.blade.php`
- Використовують Blade-синтаксис для динамічного контенту
- Можуть наслідувати інші шаблони через `@extends`

### Blade-директиви:
```blade
{{ $variable }}              {{-- Виведення з екрануванням --}}
{!! $html !!}                {{-- Виведення без екранування --}}
@if($condition) ... @endif    {{-- Умовний оператор --}}
@foreach($items as $item)    {{-- Цикл --}}
@include('partials.header')   {{-- Підключення частини --}}
@extends('layouts.app')       {{-- Наслідування layout --}}
```

### Приклад:
```blade
<h1>{{ $product->name }}</h1>
<p>Ціна: {{ $product->price }} грн</p>
@if($product->is_active)
    <span class="badge">Активний</span>
@endif
```

---

## 🛣️ Маршрут (Route)

**Маршрут** - це правило, яке визначає зв'язок між URL-адресою та методом контролера. Він вказує, який контролер та який його метод обробляють певний HTTP-запит (GET, POST, PUT, DELETE тощо).

### Основні характеристики:
- Зберігаються в `routes/web.php` (веб-маршрути) або `routes/api.php` (API-маршрути)
- Визначають URL, HTTP-метод, контролер та action
- Можуть мати параметри (наприклад, `{id}`)
- Можуть мати імена для зручної генерації URL

### Типи маршрутів:
```php
// Простий маршрут
Route::get('/products', [ProductController::class, 'index']);

// Маршрут з параметром
Route::get('/products/{id}', [ProductController::class, 'show']);

// Resource маршрути (створює всі CRUD маршрути)
Route::resource('products', ProductController::class);

// Маршрут з ім'ям
Route::get('/products', [ProductController::class, 'index'])
    ->name('products.index');
```

### HTTP-методи:
- `GET` - отримання даних
- `POST` - створення нового ресурсу
- `PUT/PATCH` - оновлення ресурсу
- `DELETE` - видалення ресурсу

---

## 🔄 Взаємодія компонентів

```
Користувач → Маршрут → Контролер → Модель → База даних
                                    ↓
                                  View ← Дані
                                    ↓
                                 Користувач
```

### Приклад повного циклу:

1. **Користувач** відкриває `/products`
2. **Маршрут** `Route::get('/products', ...)` перехоплює запит
3. **Контролер** `ProductController::index()` викликається
4. **Модель** `Product::all()` отримує дані з БД
5. **Контролер** передає дані у **View** `products.index`
6. **View** відображає HTML користувачу

---

## 📚 Додаткові терміни

### ORM (Object-Relational Mapping)
Технологія, яка дозволяє працювати з базою даних через об'єкти замість SQL-запитів.

### Eloquent
ORM, вбудований у Laravel, який дозволяє працювати з БД через моделі.

### Blade
Шаблонізатор Laravel, який дозволяє використовувати PHP у HTML-шаблонах.

### Route Model Binding
Автоматичне отримання моделі за параметром маршруту (наприклад, `{product}` автоматично знаходить `Product`).

### Mass Assignment
Можливість заповнити кілька полів моделі одночасно через масив.

---

**Примітка:** Ці визначення базуються на Laravel 10.x. Для інших версій деякі деталі можуть відрізнятися.

