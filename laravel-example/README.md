# 🚀 Приклад Laravel MVC проєкту

Цей проєкт демонструє повну реалізацію MVC архітектури в Laravel з усіма необхідними компонентами.

## 📁 Структура проєкту

```
laravel-example/
├── app/
│   ├── Http/
│   │   └── Controllers/
│   │       └── ProductController.php    # Контролер
│   └── Models/
│       └── Product.php                   # Модель
├── database/
│   ├── factories/
│   │   └── ProductFactory.php           # Фабрика
│   ├── migrations/
│   │   └── 2024_01_01_000001_create_products_table.php  # Міграція
│   └── seeders/
│       └── ProductSeeder.php             # Сідер
├── resources/
│   └── views/
│       ├── layouts/
│       │   └── app.blade.php             # Базовий layout
│       └── products/
│           ├── index.blade.php           # Список продуктів
│           ├── show.blade.php            # Один продукт
│           └── create.blade.php          # Форма створення
└── routes/
    └── web.php                           # Маршрути
```

## 🎯 Компоненти проєкту

### ✅ Міграція
- **Файл:** `database/migrations/2024_01_01_000001_create_products_table.php`
- **Призначення:** Створює таблицю `products` в базі даних

### ✅ Модель
- **Файл:** `app/Models/Product.php`
- **Призначення:** Представляє таблицю `products` та дозволяє працювати з даними

### ✅ Фабрика
- **Файл:** `database/factories/ProductFactory.php`
- **Призначення:** Генерує тестові дані для продуктів

### ✅ Сідер
- **Файл:** `database/seeders/ProductSeeder.php`
- **Призначення:** Заповнює базу даних початковими даними

### ✅ Контролер
- **Файл:** `app/Http/Controllers/ProductController.php`
- **Призначення:** Обробляє HTTP-запити та повертає відповіді

### ✅ View (Шаблони)
- **Файли:** `resources/views/products/*.blade.php`
- **Призначення:** Відображають HTML-розмітку для користувача

### ✅ Маршрути
- **Файл:** `routes/web.php`
- **Призначення:** Визначають зв'язок між URL та методами контролера

## 🚀 Як використовувати

1. **Встановіть Laravel** (якщо ще не встановлено):
   ```bash
   composer create-project laravel/laravel my-project
   ```

2. **Скопіюйте файли** з цієї директорії у ваш Laravel проєкт

3. **Запустіть міграцію**:
   ```bash
   php artisan migrate
   ```

4. **Запустіть сідер**:
   ```bash
   php artisan db:seed --class=ProductSeeder
   ```

5. **Запустіть сервер**:
   ```bash
   php artisan serve
   ```

6. **Відкрийте браузер**: `http://localhost:8000/products`

## 📚 Детальна інструкція

Повна покрокова інструкція знаходиться у файлі `LARAVEL_INSTRUCTION.md` в корені проєкту.

